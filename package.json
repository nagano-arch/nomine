{
  "name": "nomine",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:sandbox": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "dev:d1": "wrangler pages dev dist --d1=nomine-production --local --ip 0.0.0.0 --port 3000",
    "dev:lambda": "NODE_ENV=development tsx src/lambda.ts",
    "build": "vite build",
    "build:lambda": "tsc && sam build",
    "preview": "wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist",
    "deploy:prod": "npm run build && wrangler pages deploy dist --project-name nomine",
    "deploy:lambda": "./scripts/deploy.sh",
    "deploy:lambda:dev": "./scripts/deploy.sh dev",
    "deploy:lambda:prod": "./scripts/deploy.sh production",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:create": "wrangler d1 create nomine-production",
    "db:migrate:local": "wrangler d1 migrations apply nomine-production --local",
    "db:migrate:prod": "wrangler d1 migrations apply nomine-production",
    "db:migrate:postgres": "psql $DATABASE_URL -f migrations-postgres/all.sql",
    "db:console:local": "wrangler d1 execute nomine-production --local",
    "db:console:prod": "wrangler d1 execute nomine-production",
    "clean-port": "fuser -k 3000/tcp 2>/dev/null || true",
    "test": "curl http://localhost:3000",
    "git:init": "git init && git add . && git commit -m 'Initial commit'",
    "git:commit": "git add . && git commit -m"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1012.0",
    "@aws-sdk/s3-request-presigner": "^3.1012.0",
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/qrcode": "^1.5.6",
    "@vendia/serverless-express": "^4.12.6",
    "aws-lambda": "^1.0.7",
    "bcryptjs": "^3.0.3",
    "hono": "^4.12.8",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.20.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@hono/node-server": "^1.19.11",
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "@types/aws-lambda": "^8.10.161",
    "@types/pg": "^8.18.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
